<%= form_for(@arizona_customer_note) do |f| %>
  <% if @arizona_customer_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arizona_customer_note.errors.count, "error") %> prohibited this arizona_customer_note from being saved:</h2>

      <ul>
      <% @arizona_customer_note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
